#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 *_strlen - Returns the length of a string
 *
 * @s: character var
 *
 * Return: 0
 */

int _strlen(char *s)
{

	int i =  strlen(s);

		_putchar(i + '0');


return (0);
}
